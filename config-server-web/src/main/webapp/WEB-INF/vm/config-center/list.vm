<!DOCTYPE html>
<html lang="zh-ch">
<head>
    <title>新项目</title>
    <meta charset="utf-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no, minimum-scale=1.0, maximum-scale=1.0"/>
    <meta name="description" content="地贸"/>

    <link href="/assets/css/bootstrap.min.css" rel="stylesheet">
    <script src="/assets/js/json2.js"></script>
    <script src="/assets/js/jquery.min.js"></script>
    <script src="/assets/js/bootstrap-treeview.min.js"></script>
</head>
<body>
<span>启动成功</span>

<div class="container">
    <div class="row">
        <div class="col-sm-4">
            <h2>树状结构</h2>

            <div id="treeview" class=""></div>
        </div>
        <div class="col-sm-8">
            <h2>详细内容</h2>

            <div id="detailDiv" class=""></div>
        </div>
    </div>
</div>
<script>
    jQuery.get("/config-center/data", function (data) {
        console.log(data);
        var tree = $("#treeview").treeview({
            data: data,
            onNodeSelected: function (event, node) {
                //showDetail(event, node);
                console.log("checked" + node.text + "--" + node.href + "--" + node.id+"--"+node.fullPath);
            },
            onNodeUnselected: function (event, node) {
                console.log("unchecked" + node.text)
            }
            //,enableLinks: true
            , showTags: true
        });
    })

    /**
     * 显示详情
     */
//    function showDetail(event, node) {
//        console.log("fullPath=" + node.fullPath)
//        $.get("/config-center/detail", {fullPath: node.fullPath}, function (data) {
//            console.log(data);
//        })
//    }

</script>
</body>
</html>